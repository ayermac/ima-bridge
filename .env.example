# IMA OpenAPI credentials used by future sync/import features.
# Copy this file to `.env` and fill in your own values.
#
# The app reads `.env` and `.env.local` from the project/app root.
# Do not commit real credentials.

IMA_OPENAPI_CLIENTID=
IMA_OPENAPI_APIKEY=

